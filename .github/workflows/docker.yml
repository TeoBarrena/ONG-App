name: Build and Push Docker Image

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: docker build . -t centro-salud:latest

      # Descomentá las siguientes secciones si querés subir
      # la imagen a Docker Hub (requiere secrets configurados
      # en Settings > Secrets de tu repo de GitHub)

      # - name: Login to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Push to Docker Hub
      #   run: |
      #     docker tag centro-salud:latest ${{ secrets.DOCKERHUB_USERNAME }}/centro-salud:latest
      #     docker push ${{ secrets.DOCKERHUB_USERNAME }}/centro-salud:latest
